package stepdefinition;

import org.junit.Assert;
import org.openqa.selenium.By;

import io.cucumber.java.en.Given;
import io.cucumber.java.en.Then;
import io.cucumber.java.en.When;

public class Account_Edw extends BaseClass {
	/*
	 * @Given("I open {string} browser") public void i_open_browser(String browser)
	 * { openbrowser(browser); }
	 * 
	 * @Given("I visit {string} environment") public void i_visit_environment(String
	 * env) { openApplication(env); }
	 * 
	 * @When("I enter {string} username") public void i_enter_username(String uname)
	 * { driver.findElement(By.id("username")).sendKeys(uname); }
	 * 
	 * @When("I enter {string} password") public void i_enter_password(String
	 * password) { driver.findElement(By.name("pw")).sendKeys(password); }
	 * 
	 * @When("I click the {string} button") public void i_click_the_button(String
	 * buttonValue) { driver.findElement(By.xpath("//input[@value='" + buttonValue +
	 * "']")).click(); }
	 * 
	 * @Then("I should see the dashboard") public void i_should_see_the_dashboard()
	 * {
	 * Assert.assertTrue(driver.findElement(By.id("phHeaderLogoImage")).isDisplayed(
	 * )); }
	 * 
	 * @Then("I should see the account number field") public void
	 * i_should_see_the_account_number_field(String accNumber) { boolean anField =
	 * driver.findElement(By.id("acc5")).isDisplayed(); Assert.assertTrue(anField);
	 * }
	 * 
	 * @Then("I enter {string} in the account name field") public void
	 * i_enter_in_the_account_name_field(String accName) {
	 * driver.findElement(By.id("acc2")).sendKeys(accName); }
	 * 
	 * @When("I enter {string} in the account number field") public void
	 * i_enter_in_the_account_number_field(String accNumber) {
	 * driver.findElement(By.id("acc5")).sendKeys(accNumber); }
	 * 
	 * @When("I click the save button") public void i_click_the_save_button() {
	 * driver.findElement(By.
	 * xpath("//td[@class='pbButtonb' and contains (@id,'bottomButtonRow')]//input[@value=' Save ']"
	 * )).click(); }
	 * 
	 * @Then("I should see the record is saved") public void
	 * i_should_see_the_record_is_saved() { boolean myAccount =
	 * driver.findElement(By.xpath("//*[@id=\"contactHeaderRow\"]/div[2]/h2")).
	 * isDisplayed(); Assert.assertTrue(myAccount); }
	 * 
	 */
}
